{
  "_comment": "配置你的 MCP 服务器和 toolPolicy。每新增 MCP 需在 servers 和 managedMcpServers 各加一条。API key 放在同目录 .env 文件。",
  "port": 3100,
  "host": "localhost",
  "https": {
    "key": "./certs/localhost-key.pem",
    "cert": "./certs/localhost.pem"
  },
  "servers": [],
  "managedMcpServers": []
}
